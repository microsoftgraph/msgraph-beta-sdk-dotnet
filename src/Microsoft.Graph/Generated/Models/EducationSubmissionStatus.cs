namespace Microsoft.Graph.Beta.Models {
    public enum EducationSubmissionStatus {
        Working,
        Submitted,
        Released,
        Returned,
        UnknownFutureValue,
        Reassigned,
    }
}
